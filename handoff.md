# Portfolio 本地开发交接

最后更新：2026-09-12。本轮实现与验证先在本地完成；用户随后明确授权将此次更新提交并推送到 GitHub。

## 项目与工作范围

- GitHub 来源：`DemiZovo/portfolio`，同步基线 `8814d42`。
- 主目录：`D:\AAA-workspace\portfolio`。隔离开发工作树：`D:\AAA-workspace\DemiZ\.portfolio-work`，本地分支 `codex/article-editor`。
- 本轮完成加载资源减重、导航反馈及文章编辑第一版。宝物柜仍待确定内容与形式，没有擅自定稿；Friends、首页新增个人内容等旧设想未扩展为本轮需求。
- 此次代码更新按用户授权提交并推送到 GitHub；未创建 PR，未执行手动部署或线上数据库操作。代码默认继续读 Markdown。

## 已实现

### 加载和切页

- 首页字体保留原来的寒蝉圆体外观，将完整字库 2,638,560 bytes 换为介绍文案子集 14,100 bytes，减少约 99.5%。原始字库保留为源文件，不由页面引用。
- 字体重建：`scripts/subset-intro-font.py`，需 fonttools/brotli。改中英文 `home.intro` 后重新生成子集。
- 轮盘导航根据悬停/键盘选择预取，切页过程中显示状态；新增路由 loading/error 与重试。
- 滚动显现只作用于首页下方更新区域；其他页面和编辑器内容不等待显现动画。
- 内容读取统一异步化，React cache 合并单次渲染内重复读取。数据库公开读取使用 5 分钟缓存及 `public-articles` 标签；编辑后主动失效页面和发现入口。

### 文章编辑

- `/zh/write`、`/en/write`：站长登录、文章列表和筛选、新建、编辑、Markdown 预览、导出、草稿保存、发布、撤回、回收站、恢复与永久删除。
- 页脚和 Workshop 提供入口；站长登录后，公开页面显示管理工具条，可从文章页进入对应编辑器。
- 表单管理标题、摘要、固定 slug、类型、日期、分类、标签、英文标题/摘要和封面路径；已有其他元数据保留。首版不做图片上传、任意页面搭建或分类管理。
- 工作副本和公开快照分开，保存已发布文章的修改不会直接改变公开内容。恢复回收站内容默认仍为草稿。
- 原子 SQL 操作带版本检查，防止旧窗口覆盖新版本；稳定 slug 保持 URL/阅读标记不变。
- Supabase Auth 服务端验证 + `site_admins` 白名单 + RLS。HttpOnly/Secure/SameSite cookie，有效期最多一小时；编辑器中可重新登录。写接口检查同源与请求大小。
- 私有接口与预览 no-store/noindex；原始 HTML 丢弃，Markdown 活跃协议链接移除。
- 保存失败保留输入；离开有修改页面提示；未保存内容暂存 sessionStorage，可选择恢复，主动退出清除。手动 Markdown 导出始终可用。
- 新增文章和标签允许构建后访问；发布/撤回/删除后首页、列表、分类、归档、搜索、RSS、sitemap 保持一致。

## 数据与正式启用边界

- `supabase-editor.sql` 新建 `editor_articles`，不依赖旧 DemiZ 目录中的 `content_entries` 草案，也不修改留言板表。
- `scripts/export-editor-import.mjs OUTPUT.sql` 只生成可审阅 SQL，保留现有 Markdown 的元数据/正文/slug/草稿状态，遇到已有 slug 不覆盖。本地已生成并导入 5 篇原文章到内存测试数据库。
- 正式启用还需要执行 SQL、创建站长账号、导入真实数据与配置环境变量，详见 `EDITOR_SETUP.md`。这些外部步骤本轮未执行。
- 切到 `CONTENT_SOURCE=supabase` 后数据库是唯一文章来源，查询失败不回退 Markdown，避免已撤回内容重新出现。
- 默认 `CONTENT_SOURCE=markdown` 时公开站点保持原行为，编辑入口明确显示尚未配置。

## 验证记录与限制

- TypeScript、数据库模式及默认 Markdown 模式生产构建通过；默认模式生成 90 个页面/路由。`git diff --check` 通过。
- `scripts/test-editor-db.mjs` 使用 PGlite 执行实际 SQL：验证非站长拒绝、RLS、禁止绕过 RPC 写入、重复 slug、版本冲突、快照隔离与完整状态变更。
- `scripts/test-editor-api.mjs` 对本地生产服务验证：cookie、未登录/非站长/跨站写入拒绝、预览过滤、新 URL 无需构建、草稿不公开、发布撤回删除后的页面和索引一致、冲突和回收站。测试通过。
- 浏览器实际验证：登录、新建草稿、保存、预览、发布、通过 query 直接打开文章、离开未保存内容提示；桌面和 390px 窄屏、浅色/深色排版通过。sessionStorage 恢复入口已实现，自动化浏览器拦截刷新确认，未完成强制刷新后的整段 UI 恢复实测。
- 数据库模式全站检查通过 78 个 sitemap URL、87 个内部目标（含一篇本地 UI 测试文章）。修复验证脚本不能映射正式域名到本地预览的问题。
- 最终默认 Markdown 模式全站检查通过 76 个 sitemap URL、85 个内部目标；浏览器确认未配置状态与字体子集外观正常。最终浏览器资源中只有 14,100 bytes 的字体，未发现测试假凭证。
- Next loading 流式响应遇到不存在/已撤回文章时可能先返回 HTTP 200，随后输出 notFound 和 noindex；测试同时校验正文缺席与 noindex，不仅依赖状态码。
- 原版本地生产主要路由 TTFB 约 5–29ms，最终版本约 8–46ms；两轮受本机负载影响，未显示服务端响应时间改善。没有线上网络/CWV 或相同网络条件下 LCP/INP 实测，不宣称线上卡顿已经全面解决；已确认的性能改善是字体资源显著减小和导航等待反馈。
- 测试用 `scripts/editor-fixture.mjs` 只监听 127.0.0.1，独立内存数据库，假凭证仅用于本地测试。应用不导入测试 fixture；它不是正式存储或登录方式。

## 下一步

1. 用户选择启用实际 Supabase 后，按 `EDITOR_SETUP.md` 接入并执行真实账号/存储验收；不要因为本地测试通过就宣称生产配置已完成。
2. 在线上和实际移动网络采集性能指标，再决定是否继续拆分客户端组件、调整缓存或优化其他资源。
3. 宝物柜先收集真实藏品类型与素材，再选择轻量图片展架或工具链接目录，避免新增重型 3D 首屏。
4. 本轮用户已授权推送代码；后续 PR、手动部署与线上数据库变更仍按用户指令执行。
